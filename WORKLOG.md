## 2026-09-22 (вечер) — студийные портреты команды (feat/site-v3)

**Сделано**
- Пять портретов команды перегенерированы в одном студийном формате (серый фон, тёмно-синяя рубашка с «RAX PRO») с реальных фото как референсов: Higgsfield `gpt_image_2_5`, 3:4, `image_references` = `/works/expert*`. Файлы `public/images/team/{xurshidbek,islombek,nurilla,murodbek,shaxzoda}.jpg` (900×1200), `T.ru/uz.specialists.img` переключены.
- Герой `/experts`: тайл с пятью клонами одного лица → полоса из пяти новых портретов (`head1.jpg`), сток-мужчина в жилете → Хуршидбек (`head2.jpg`). `command.png`, `head1.png`, `head2.png` удалены.
- Из `lib/i18n.js` убран мёртвый `export const translations` (никем не импортировался, дублировал список команды).

- Первый экран `/experts` по правке Умида — только текст (заголовок, абзац, кнопка на форму `/#zayavka`), коллаж справа и его файлы `head1–4` удалены; inline-CSS блок заменён на Tailwind.

**Грабли**: Gemini nano_edit — HTTP 402 (кредиты кончились), генерация через Higgsfield MCP. Первый прогон был без референсов Исломбека/Нуриллы/Муродбека/Шахзоды — выдуманные лица; переделано с реальных фото.

**Осталось**
- [ ] Лица — ИИ-версии реальных фото; если клиент хочет настоящую фотосессию — заменить файлы в `public/images/team/`, имена те же.

# WORKLOG — raxpro-site

## 2026-09-22 (день) — мобилка по правилам, сравнение «заводские vs кустарные» v2, форма по разбору, футер (feat/site-v3)

**Сделано**
- Мобилка: кейсы — карточка в один экран iPhone (фото 16/9, история «Задача и решение» свёрнута кнопкой, превью-лента и мини-фото скрыты, desc блока скрыт на телефоне через `SplitHead hideDescOnMobile`); направления — горизонтальная лента со snap (82vw, край следующей карточки виден), «для кого» скрыт, описание clamp-3; цифры «1000+…» компактнее. `scrollbar-none` теперь реально определён в globals.css.
- `components/Comparison.jsx` (новый, вместо inline-блока и `cmp*` в i18n): таблица пар в двух группах — «Материал и гарантия» + «Чего у сварного нет в принципе» (ярусы, разборка, наращивание, замки), строка про безопасность, карточка «Да, сварной дешевле на старте» + CTA в конструктор. «Часто в наличии» → «Всё в наличии в любом объёме». Марка стали/толщина стойки — PLACEHOLDER (нет данных).
- Форма: заголовок «Получите план склада и точную цену — бесплатно», текст без противоречия 24 ч / 1–2 дня; обязателен только телефон; комментарий свёрнут в «+ Добавить детали»; select стилизован; honeypot `website` (API молча отвечает ok); статус под кнопкой по часам Ташкента (`lib/workingHours.js`, Пн–Сб 9–18: «Ответим за 5 минут» / «нерабочее время — ответим завтра|сегодня|в понедельник с 9:00»); Telegram/WhatsApp — кнопки; согласие со ссылкой на политику; `dataLayer` события `lead_submit`, `lead_messenger_click`, `lead_thank_you`. Шаг «5 мин» из колонки убран (дубль), адрес из блока убран. /thank-you: «Продолжить в Telegram» главной кнопкой, tel: исправлен.
- Сертификаты: закрытый `<details>` торчал краем карточек — глобально `details:not([open]) > :not(summary){display:none}`.
- Футер: кнопка-звезда «Отзывы» убрана; «Покупателю» без дублей «О компании»/«Контакты»; «Разделы» без «Продукция»/«Каталог»; «Мезонин» добавлен в продукцию; «Эксперты» → «Команда» (uz «Jamoa»).

**Проверено**: build ок; 393 (iPhone) секции кейсов/направлений/формы; 1440 сравнение.

**Осталось**
- [ ] Фото «Заводские RAXPRO» в сравнении — похоже на сток; нужно фото реальной стойки со склада.
- [ ] Марка стали и толщина стойки для таблицы сравнения — спросить у Хуршида.
- [ ] Имя в форме сделано необязательным — решение клиента, можно вернуть `required`.
- [ ] Остальные секции мобилки: этапы (2582 px), «почему» (3035), отзывы (2868), FAQ, блог — следующий проход.

## 2026-09-22 (ночь, 3) — калькулятор ниже, «Из чего складывается цена» упрощён, «в цифрах» убран с /experts (feat/site-v3)

**Сделано**
- Страница команды `/experts`: блок «RAXPRO в цифрах» (1000+ / 10 лет / 4 т / 100 % / 40 / с 2021) удалён по Умиду; ключи `numsEyebrow/numsTitle/numsText/numsCta/stats` (ru+uz) из i18n удалены — иначе «RAXPRO в цифрах» жило в RSC-пейлоаде страницы.
- Калькулятор перенесён из-под направлений вниз: теперь Цена → Этапы → Почему RaxPro → **Калькулятор** → Сравнение → Проекты. Легко сдвинуть ещё ниже, если надо.
- `PriceBreakdown` упрощён по «слишком много информации»: таблица элементов (рамы/балки/замки/анкеры/защита) и три схемы оплаты убраны; осталось заголовок + текст + CTA слева, справа карточка «В цене уже есть» (4 пункта) и «Оплата 50 / 50» + рассрочка. `SCOPE/TERMS` из `lib/rack/company` больше не читаются на главной — там формулировка про завод-партнёр для КП.

**Проверено**: build ок; 1440/320 без сдвига, консоль чистая.

## 2026-09-22 (ночь, 2) — «мы не производим»: сквозная чистка, «после заявки» у формы, блог над футером, плашки убраны (feat/site-v3)

**Сделано**
- «Производим / изготовление / от производителя / собственное производство» → «поставляем со склада в Ташкенте / комплектация со склада» — 91 замена: layout meta, hero, футер, schema, i18n, o-kompanii, experts, kontakty, products, shop, directions, policies (оферта: «поставщик», «Сроки поставки», «отгрузка со склада 1–2 рабочих дня»), articles, uz-articles. Не трогал: отрасль клиента («производство» как тип помещения), KP-генератор (`lib/rack`, там «завод-партнёр»), сравнение «под заказ у производителя vs у нас» в «Этапах», строку про завод-производитель ISO (текст Умида).
- «Только мы / единственные / faqat RAXPRO / yagona» про 10 лет → «вдвое больше, чем обычно на рынке» — i18n (FAQ, adv, guaranteeText), o-kompanii, articles ru/uz.
- Блок «Что будет после заявки» удалён как экран; суть — колонкой у формы (`x.afterTitle/afterSteps/afterNote`): 5 мин в рабочее время Пн–Сб 9–18 · 24 ч замер · 1–2 дня план+3D+КП · «Платить за расчёт не нужно. Решение за вами». Без «никакого спама», без конструктора. Бейдж формы → «Ответим за 5 минут в рабочее время». `afterHands/afterText/afterEyebrow` удалены.
- Блог перенесён в конец, перед футером. Плашки-метки убраны: калькулятор, направления, сравнение, проекты, основатель, отзывы, FAQ, форма, контакты, блог, «Цена». Оставлены только «Этапы и сроки» и «Почему RaxPro» (там метка — имя блока). `SplitHead` — eyebrow опционален.
- Нагрузка: 3 500 кг в спецификации паллетных → 4 000 кг, одна цифра на сайт (везде «до 4 т»). **Подтвердить у Хуршида, что 4 т — правда.** «Discover Invest» → «Discovery Invest» (site.js, rack/profile.ts). «24 дней» → «24 дня» (`cmpTailDays`). Из «Этапов» и bento убраны «без производства/растаможки».

**Проверено**: build ок; 1440/390/320 ru+uz — без сдвига, консоль чистая; на странице нет «производим», «только мы», «растамож».

**Осталось**
- [ ] 4 т vs 3,5 т — подтвердить у клиента. Отгрузка «1–2 рабочих дня» в каталоге/оферте — PLACEHOLDER, подтвердить.
- [ ] Шапка: чип «Ответим за 5 минут» без «в рабочее время» (коротко) — решить, менять ли.
- [ ] PLACEHOLDER: bento, кейсы, «нам доверяют». Отзыв Умида → `searchParams.hero`, merge, `vercel --prod`.

## 2026-09-22 (ночь) — реалистичные рисунки стеллажей, минус полоса «10 лет», минус счётчик отзывов (feat/site-v3)

**Сделано**
- Рисунки bento вынесены в `components/RackFigures.jsx`: косая проекция (`D`, `FRAME`), стойки с перфорацией и пятками, задняя рама с раскосами, балки с кронштейнами, деревянные паллеты с коробками (3 раскладки, блик плёнки). Вместимость: 2 секции × 2 паллеты × 3 яруса = 12 против 4 на полу — счёт по-прежнему из `FLOOR_PALLETS`/`RACK_LEVELS`. Ярус: настил на балке, призрак балки пунктиром, стрелка; контейнер 128/180 px.
- Полоса «10 лет официальной гарантии» удалена с главной (по Умиду). Ключи `guaranteeTitle/Text`, `t.yil` в i18n остались; факт гарантии живёт в bento и FAQ.
- Блок отзывов: цифра «31 / отзыв клиентов» слева убрана, только eyebrow + заголовок + подзаголовок; `reviewsWord` из page.jsx удалён. Кнопка «Все 31 отзыв» под сеткой осталась.
- Карта сегментов (артефакт) пересчитана по всему каналу @RaxPro_otziv: 260 постов, 66 объектов — B 37 / D 13 / C 9 / A 7; поправлено «повтор только в B» (Monaer, Caffelito — C).

**Проверено**: build ок; 1440/390/320 ru+uz — без сдвига, ошибок консоли нет.

**Осталось**
- [ ] «Производство» в hero_desc, футере, направлениях, uz-ключах — после «да».
- [ ] PLACEHOLDER: bento, кейсы, «нам доверяют».
- [ ] Отзыв Умида → убрать `searchParams.hero`, merge в main, `vercel --prod`.

## 2026-09-22 (вечер) — отзывы без фото + /otzyvy, bento «Почему RaxPro», кейсы-истории, сертификаты одной строкой (feat/site-v3)

**Сделано**
- Отзывы: фото объектов из текстовых отзывов убраны, карточки одной высоты в сетке 3 кол. На главной — 11 отобранных (`FEATURED_IDS` в `lib/reviews.js`: 5 видео + 6 с конкретикой), цифра 31 остаётся, кнопка «Все 31 отзыв» → новая страница `/[lang]/otzyvy` (все 31: 8 видео · 17 переписок · 6 голосовых, ссылка на TG-канал, CTA). Футер «Отзывы клиентов» теперь ведёт на страницу, не в TG; sitemap +1.
- Блоки «Выгода» и «Не обещания, а факты» заменены одним bento `components/WhyRaxPro.jsx` по макету Умида: вместимость (рисунок «пол vs стеллаж»), наличие, гарантия 10, безопасность 4 т + 5 пунктов, окупаемость, доступ, удобство (рисунок балки), гибкость, металл (3 слоя), оплата 50/50, полоса «Сколько поместится в ваш склад?». PLACEHOLDER: ×3, 6 мес, шаг 50 мм, «Сталь 08ПС · 1,5 мм» — константы наверху файла. Ключи `income/adv/inc*/adv*` в i18n остались, не читаются.
- Кейсы по разбору: `lib/cases.js` → задача / решение / 2–4 цифры / цитата, `segment` (warehouse·shop·marketplace·production), порядок — Discovery×Radisson первым. `CaseSlider`: чипсы-фильтры с количеством, счётчик с сегментом, ряд превью всех кейсов под слайдером. PLACEHOLDER у первых четырёх: сроки/площадь/цитаты (помечены в файле). Строки «Объект/Продукт» и дубли параметров убраны.
- «Изготовлено/производство» вычищено там, где касался: подзаголовок кейсов («поставлено со склада»), FAQ (комплектация со склада, «Как быстро поставите?»), этапы Sayqal.
- Сертификаты: шесть карточек → одна строка «Завод-производитель сертифицирован по ISO 9001, 14001 и 45001» + «Посмотреть сертификаты →» (`<details>`, сканы раскрываются в 6 кол.). В блоке основателя чипы ISO заменены той же строкой и ссылкой на `#sertifikaty`.

**Проверено**: build ок, hero-тесты 13/13; 1440/390/320 — главная ru/uz и /otzyvy без сдвига, ошибок консоли нет.

**Осталось**
- [ ] «Производство» ещё живёт в hero_desc (meta), футере («Замер, проект, производство и монтаж»), карточке направлений, uz-ключах (`ishlab chiqaruvchisi`, «Oʻz ishlab chiqarishimiz») — общий проход по слову после «да» Умида.
- [ ] Заменить PLACEHOLDER: цифры bento, кейсы (сроки, площадь, цитаты), «нам доверяют».
- [ ] Отзыв Умида → убрать `searchParams.hero`, merge в main, `vercel --prod`.

## 2026-09-22 — блок «нам доверяют» по разбору, сегменты героя, загрузка без постера (feat/site-v3)

**Сделано**
- «Нам доверяют»: плашка «Цифры» убрана, цифра в заголовке «**1000+** складов в Узбекистане уже работают на наших стеллажах» + подзаголовок; противоречия сняты (2000+ клиентов vs 1000+ проектов, «с 2021» рядом с «10 лет гарантии», двойное «по Узбекистану»). Новые ключи `trustTitle / trustText / trustStats` (ru/uz), `stats` остался для /experts.
- Шесть цифр: 1000+ складов · 50 000+ паллетомест · 5 дней замер→сдача · 4,9★ Google 120 отзывов · 40 человек в штате · 12 регионов. **PLACEHOLDER** — Умид разрешил заглушки, заменить фактами клиента (помечены в `lib/i18n.js`).
- Логотипы → мини-кейсы: `CLIENT_LOGOS[].note {ru,uz}` в `lib/site.js`, `LogoMarquee` принимает `lang` и рисует строку «Radisson · склад хозблока, 180 м²». Объекты и цифры — PLACEHOLDER.
- Герой: сегменты + «Набивной» (→ /napravleniya/nabivnye-stellazhi) и «Мезонин» (→ /katalog/mezonin); сегмент с `href` вместо `type` идёт на страницу, а не в конструктор. Кикер «Стеллажи для» на своей строке — пять чипов в ряд на 1440.
- Баг загрузки героя (Умид: «появляется картинка, потом 3D»): стартовый режим `static` показывал постер `/works/hero.jpg` и все четыре стадии столбиком на время загрузки three.js. Теперь SSR сразу в раскладке `live`, холст проявляется по `data-ready` с fade 0.9 s; `static` — только reduced-motion или падение WebGL.
- «Из чего складывается цена»: убран «Пример из прайса 7 032 128 сум за секцию» (по замечанию Умида).
- Блок «Циклы» заменён на «Этапы и сроки» (`components/Stages.jsx`, макет Умида): «Склад под ключ за 7 дней. Всё в наличии — завод не ждём», тёмная карточка склада с фото (Higgsfield gpt_image_2_5, `public/images/stock.jpg`) и «150 тонн», сравнение «под заказ 14–28 дней / у нас 7 дней», полоска дней 1+2+1+3 и четыре карточки этапов, третья тёмная «Комплектация со склада». Карточки проявляются по скроллу: на ≥1024 — по доле прогресса ряда через нижнюю половину экрана (полоска дня заливается, потом карточка), на телефоне — каждая при входе в экран; reduced-motion — всё сразу. `t.steps/procEyebrow/procTitle/procText` в i18n остались (никем не читаются). PLACEHOLDER: 7 дней, 150 тонн, 14–28 дней — константы наверху файла.
- Сравнение сроков в «Этапах» → диаграмма на одной шкале дней (`LeadTimeChart` в Stages.jsx, по замечанию «нужно явно визуально»): шкала 0–28, завод = 14 серых + 14 штрихованных (задержка) + 3 монтаж = «до 31 дня»; мы = 1+2+1+3 = 7 (комплектация тёмная, как её карточка), дальше пунктирный хвост «склад уже работает · 24 дня»; пилюля «На 24 дня раньше» считается из констант. Полосы вырастают при входе в экран. На телефоне итог — в строке подписи, тики — только числа.
- Мобильный герой (<1024px) переделан: ничего не липнет, обычная высота. Сверху текст на чистом фоне (сегменты, H1, описание, CTA), под ним маленькая 3D-модель (`clamp(230px, 62vw, 340px)`), которая собирается сама по времени — вперёд 11 с, пауза 2,8 с, откат 1,2 с, снова (`AUTO`, `autoProgress` в RackHero.jsx); под моделью заголовки стадий сменяются в том же ритме (+ новая стадия «01 / Замер» в heroCopy — только телефон) и таймлайн, клик по этапу перематывает цикл. Раскладка — `display: contents` у `.rack-editorial` и `order` в `.rack-sticky`; `.rack-stages` — обёртка для наложения панелей. Холст на телефоне `clamp(250px, 76vw, 420px)` без overflow/скругления, орбитальная камера на холсте <700px дальше на 10 % (`width` в rackScene) — Умид: «края 3D-объекта обрезаются», проверено на пяти точках цикла. Десктоп не менялся.
- Телефон: фиксированная панель «Рассчитать / Позвонить» (`MobileCta`) снята с layout, виджет опущен в `bottom-5`; файл компонента оставлен.
- Шапка на телефоне прячется при скролле вниз и возвращается при движении вверх (`hidden` в Header.jsx, порог 80px, шаг 6px, открытое меню не прячется); десктоп всегда на месте.
- Направления на главной: шестая карточка «Мезонин» (`mezzanineCard` в `lib/directionCards.js`, фото `products/gen/mezzanine-1.jpg`, → `/katalog/mezonin`, «Рассчитать с инженером»). Раньше пятая карточка Drive-in растягивалась на всю ширину — Умид: «эта карточка огромная». `dirText` — «шесть типов».

**Проверено**: build ок, hero-тесты 13/13; 1440/320 — scrollWidth ровный, ошибок консоли нет.

**Осталось**
- [ ] Заменить PLACEHOLDER-цифры и мини-кейсы логотипов фактами клиента (через Умида).
- [ ] Отзыв Умида → убрать `searchParams.hero`, merge в main, `vercel --prod`.

## 2026-09-21 — правки по preview: направления после цифр, каталог 6 продуктов (feat/site-v3)

**Сделано**
- Секция «Наши основные направления работы» перенесена сразу после блока «Нам доверяют / цифры» (перед калькулятором).
- Карточка направления: фото в своей зоне `aspect-[16/10]` (иконка, стрелка, пилюля цены поверх фото), описание/характеристики/CTA — на белой панели ниже. Раньше текст лежал поверх фото под градиентом — фото не читалось.
- Каталог: 6 позиций. К трём с ценой добавлены `nabivnoy-stellazh-drive-in`, `torgovyy-stellazh`, `mezonin` с `price: null`, `priceMode: "project"` — бейдж «Под заказ», «Цена по проекту», кнопка «Рассчитать с инженером» → `#zayavka`, у торгового ещё «Собрать в конструкторе» (`konType: retail`). Размеры/нагрузки — из `lib/rack/catalog.ts` и `lib/directions.js`.
- `PRICED_PRODUCTS` / `isProjectPriced` в `lib/products.js`: фид Merchant Center (`lib/feed.js`), заказ (`app/api/order`) и минимальная цена в карточках направлений берут только позиции с ценой; `productOfferSchema` без `offers`/размеров у позиций «по проекту».
- Фото Higgsfield gpt_image_2_5 4:3 1168×880: `public/products/gen/{drivein,retail,mezzanine}-{1,2,3}.jpg`.
- SHOP: лид и SEO каталога — «шесть видов стеллажей», новые ключи `toOrder / byProject / byProjectNote / withEngineer / inKonstruktor` (ru/uz).

**Решения**
- Мезонин без страницы направления (`directionSlug: null`) — направлений на сайте пять, отдельную страницу не выдумывал.
- «Что входит в цену» у позиций по проекту скрыт — состав задаёт КП.

**Проверено**: build ок, hero-тесты 13/13; 320/1440 главная, каталог, карточки товара — без сдвига, ошибок и битых картинок.

**Осталось**
- [ ] Отзыв Умида → убрать `searchParams.hero`, merge в main, `vercel --prod`.

## 2026-09-21 — волна 3 preview v3: отзывы, кейсы, фото, футер, тексты героя (feat/site-v3)

**Сделано**
- Отзывы: один блок вместо двух (OpinionsSlider удалён). `lib/reviews.js` — 31 отзыв из @RaxPro_otziv: 8 видео (`public/reviews/video/r*.mp4` + постеры, 38 МБ), 17 переписок с кропами объектов без телефонов (`public/reviews/photos/`), 7 голосовых. `components/Reviews.jsx`: лента видео 9:16 + masonry цитат, «Показать ещё». Кнопка «Все отзывы в Telegram» убрана.
- Кейсы: `lib/cases.js` (11 кейсов, факты только из постов канала) + `components/CaseSlider.jsx` — фото слева, детали справа, стрелки/свайп/клавиатура. ProductSlider удалён.
- Фото Higgsfield (gpt_image_2_5, 1168×880 4:3): каталог 3 кадра на продукт (`public/products/gen/`), направления (`public/directions/`), сравнение заводские/кустарные (`public/images/cmp-*.jpg`), офис для контактов (`public/images/office.jpg`).
- Контакты: фото офиса + карточка телефонов/почты/часов (добавлен ключ `hours` в i18n — раньше подпись была пустой), соцсети только в футере. Футер переписан: фраза «Соберём ваш склад.», пилюля-CTA, волосяные колонки, белый лого, соцсети иконками.
- Герой по критике Умида: заголовок «Больше товара на той же площади склада.», сегменты «Склад / Магазин / Архив-гараж» → `/konstruktor?type=pallet|retail|archive`, CTA «Узнать вместимость моего склада» → конструктор, чипы без дублей (гарантия — в тексте, «5 минут» — в шапке), один срок: конструктор 1 минута · точный расчёт 24 ч. Стадия «Вместимость»: без стеллажей {floor} паллет → со стеллажами {positions} паллетомест → ×{ratio} (14 → 50 → ×3,6, считается из rackModel), масштаб «Склад 17 × 13 м · потолок 7,4 м» в тексте и на плашке. «По документу» → «по договору» везде (42 замены, uz «shartnoma boʻyicha»).
- Карточки направлений: текст в потоке (`pt-44/56`), не absolute — на 320px заголовок вылезал за верх карточки; градиент `via-navy-900/80` для читаемости на новых фото.
- Мелочи: FAQ в две колонки с иконками, «Все статьи» кнопкой, анимированный scroll hint, мобильная тень героя усилена (факты читались поверх коробок).

**Решения**
- Кейсы без цифр «сэкономили N» — их нет в источниках; строки только те, что есть в постах. Низкие кадры из видео (352×640) у STAR/Sayqal/Qoʻqon/Monaer — при необходимости апскейл.
- `?hero=` переключатель оставлен до «да».

**Грабли**
- `IcoShield` убрал из импорта раньше, чем из массива — 500 на главной; dev-лог `/tmp/rax-dev.log`. Первый рендер после старта dev идёт без CSS — scrollWidth 1608 ложный, повторить снимок.
- site-qa на preview: 320px «не открылся» — это Vercel Live toolbar (`vercel.live/_next-live/feedback`) держит открытый `fetch('/ru')`, networkidle не наступает. На проде тулбара нет; 320 проверял своим скриптом: scrollW 320, ошибок и битых картинок нет.
- Плашка вместимости скрыта <600px намеренно (`.rack-capacity { display:none }`), цифры на мобиле — в фактах стадии.

**Осталось**
- [ ] Отзыв Умида по preview → убрать `searchParams.hero`, merge в main, `vercel --prod`.
- [ ] Логотипов клиентов 8, портреты команды, таблица нагрузок — от клиента.

## 2026-09-21 — правки Умида по preview v3: первый экран и «нам доверяют» (feat/site-v3)

**Сделано**
- Герой: оффер первым — «Склад под ключ за 1 день.» + одна строка объяснения (замер/проект бесплатно, монтаж до 300 м² за день, гарантия 10 лет); убраны цена за паллетоместо, три цифры, «замер бесплатно» под кнопкой, факт «Zn + RAL», подпись «Иллюстрация планировки…». Кнопки: «Рассчитать стоимость» → #kalkulyator, «Оставить заявку» → #zayavka (вместо «Спроектировать онлайн»).
- Стадии Замер → Проект → Сборка → Вместимость — таймлайн с точками и заливкой линии (`.rack-navigation`, `data-done`).
- Сцена: перспективная орбита камеры как в feat/rack-hero (`ORBIT` в `rackScene.js`), стены/пол/колонны затемнены (`sceneAssets.js` #7f97ab, floor 0x5f7488), гемисфера 1.0.
- Хедер: корзина убрана. «Нам доверяют»: белый фон на всю ширину, логотипы 220×80 цветные, «2000+ клиентов по Узбекистану» (`clientsCount`/`clientsText` в i18n).
- Тесты героя 13/13. Из `heroCopy.js` удалены неиспользуемые ключи.
- Вторая волна (тот же день): оффер главной = «Стеллажи и системы хранения полного цикла» + текст, 4 галочки и бейдж «от 1,2 млн сум» (в портрете галочки/бейдж скрыты — накладывались на фигуры); кнопки «Рассчитать стоимость за 1 минуту» → #kalkulyator и «Смотреть каталог» → /katalog. Из сцены убраны подпись «Ваше помещение», «17 м / 13 м» и размерные линии — лазер и замерщики остались. Тёмный блок «RAXPRO в цифрах» слит с «Нам доверяют» в один белый: заголовок + «2000+ клиентов…» обычным текстом слева, 6 цифр сеткой с волосяными линиями справа, логотипы лентой снизу.

**Осталось**
- [ ] Больше логотипов — файлов только 8 (`public/clients/logo_00..07.png`), остальные нужны от клиента.
- [ ] Отзыв Умида по preview → убрать `searchParams.hero`, merge в main, `vercel --prod`.

## 2026-09-21 — v3 по отчёту: конструктор, герой, направления, цена, FAQ (ветка feat/site-v3 от feat/rack-hero-detail)

**Сделано**
- `/ru|uz/konstruktor` — три шага на движке `lib/rack/` (`lib/rack/konstruktor.ts`, `components/konstruktor/*`): паллетомест, план, 3D (щипок, фигура 1,75 м), спецификация, расчётная цена; «Сохранить PDF» и «отправить коллеге» = ссылка `/tp#…`; заявка уходит с конфигурацией и фото (multipart в `/api/lead` → документ в TG). Тесты `lib/rack/konstruktor.test.mjs` 7/7.
- Главная: герой «от 643 316 сум за паллетоместо · расчётная» (`referencePerPalletPosition`), 1000+ / до 4 т / с 2021, кнопки → конструктор и #zayavka; хедер «Конструктор» (вместо «Преимущества»), «Ответим за 5 минут» у телефона; карточки направлений с «от N сум/секция», нагрузка/высота, «для кого», кнопка `/konstruktor?type=`; чипы-метрики у Bloom Shop и Caffelito; «после заявки» 5 шагов + «что на руки»; блок «Из чего складывается цена» (`components/PriceBreakdown.jsx`); FAQ 7→13.
- Страницы направлений: цена «от», кнопка в конструктор, таблица типоразмеров из `lib/rack/catalog`.
- Разведка закоммичена (md + build.py), скриншоты в .gitignore. Коммиты 9c25eff…4633e22.

**Решения**
- Расчёт на клиенте (движок и так едет в `/tp`), без API. Цифры только из lib/rack, lib/products, lib/directions, company.ts — новых чисел нет; сроки «после заявки» = те, что уже были на сайте (5 мин · 24 ч), остальное без цифр.
- `MobileCta` скрыт на `/konstruktor` — там своя липкая панель. Переключатель `?hero=` на главной оставлен до «да».
- Колонка нагрузки в таблице типоразмеров убрана: catalog (80–100 кг) и directions (100–300 кг) для архивных расходятся — ждём таблицу нагрузок от клиента.

**Грабли**
- В тесте `sections`×`rows` — секций в ряду × рядов, не общее число. `/tp#` с раскладкой грузится 4–6 с (headless без GPU).
- Dev на :3077 (3000 занят bobonchik); Next перезапускается по памяти — первый GET главной до 13 с.

**Осталось**
- [ ] Люди («Ваш инженер», команда) — нет фото и ролей, `command.png` плейсхолдер: ждём от клиента.
- [ ] Отзывы с ролью/компанией, цифры по 6 проектам, таблица нагрузок, сроки этапов — от клиента через Умида.
- [ ] После «да»: убрать `searchParams.hero`, merge в main, `vercel --prod`.

## 2026-09-20 — разведка рынка стеллажей и отчёт-страница для v3 (main не трогали)

**Сделано**
- Отчёт: https://claude.ai/artifact/8HD4vxJ6NwXsUUNYMRtqUE — генератор `research/report/build.py` → `raxpro-research.html` + 82 jpg в `research/report/img/`.
- Доска 45 экранов (UZ 4 · RU 11 · RU-конструкторы 5 · KZ 6 · мир 8 · дизайн 9) — `research/competitors/`, таблица `research/RESEARCH.md`.
- Полосы 820px по 20 сайтам — `research/conversion/`; конфигураторы desktop+iPhone — `research/conversion/cfg/`; разбор — `research/ANALIZ.md`.
- Скрипты съёмки: `~/.claude/tools/site-qa/_rax-board.mjs`, `_rax-retry2.mjs`, `_rax-strips.mjs`, `_rax-cfg.mjs`.

**Решения**
- Одно домино v3 — публичный `/konstruktor` на движке `lib/rack/` (layout/spec/pricing/RackScene/PDF/tp-ссылка уже есть в генераторе КП); цена — «за паллетоместо».
- Приоритет 1: герой (цена за паллетоместо, три цифры, две кнопки), конфигуратор, карточки направлений с «от», кейсы с м²/паллетомест, блок «после заявки».

**Грабли**
- aviastal 403, mirvalino → Facebook, topregal 401, mh-usa капча, tylko Cloudflare, vitsoe/mecalux конфигураторы 404 — в `_broken/`. Витрина Tilda не снята (поиск закрыт).
- macOS без `timeout`; `rm`/перезапись файла скриптом блокирует хук — Edit-инструментом или в `_broken/`.

**Осталось**
- [ ] «да» Умида по отчёту → ветка `feat/site-v3` от main, этапы 1–3 из раздела 08.
- [ ] От клиента: таблица нагрузок, цифры по 6 проектам, сроки этапов, письма, фото команды, политика цен.

## 2026-09-18 — люди, товар, баннер, портретная камера; правки по скрину Умида (ветка feat/rack-hero-detail)

**Сделано**
- 858101f: люди из капсул (жилеты со светоотражателями, каски, лица, локти, инструмент), idle-движение; пять вариантов груза на паллетах (коробки, плёнка, бочки, крупная коробка + мелкие); баннер RAXPRO на задней стене (слоган ru/uz); на телефоне перспективная камера с туманом вместо обрезанной ортографической плиты; `.rack-shade` светлее.
- ff2475d по замечанию Умида: «земля» 400×400 м из освещённой синей плоскости → `ShadowMaterial` (сквозь прозрачный холст идёт градиент страницы, светлая подложка ушла); ортокамера: target y 1.8→3.0, span 9.7→10.3 — верх стен больше не под меню. Стены оставлены (Умид уточнил: речь про фон объекта, не стены).
- Preview: `https://raxpro-site-git-feat-rack-hero-detail-umid-s-projects.vercel.app/ru` (uz — `/uz`). Тесты 13/13. site-qa: только чужое (аналитика в headless, title 99 симв., 320px networkidle-таймаут из-за беконов GA).

**Решения**
- Фон сцены = фон страницы, а не своя плоскость: любая освещённая плоскость тонмаппится светлее CSS-цвета.
- Опускать сцену через цель камеры, а не CSS-отступ холста — так не теряется высота холста.

**Грабли**
- Скриншоты WebGL в headless: playwright-core из euromed-deck + `--use-angle=swiftshader --enable-unsafe-swiftshader --ignore-gpu-blocklist`; скрипт `scratchpad/rax/shots.mjs`.
- Спрайты-подписи в перспективе разъезжаются в размере: в портрете масштаб 0.5, подписи размеров скрыты.

**Осталось**
- [ ] «Да» Умида → убрать `searchParams`-переключатель в `app/[lang]/page.jsx`, merge в main, `vercel --prod`.

## 2026-09-17 — детализация 3D-героя (ветка feat/rack-hero-detail)

**Сделано**
- Сцена из «макета» в цех: `sceneRoom.js` (пол со швами и жёлтой разметкой прохода, две стены-сэндвич, ворота, окна, дверь, огнетушитель),
  `sceneProps.js` (отбойники стоек, таблички «до 4 т / ярус», паллеты трёх видов, две паллеты на полу, рохля с паллетой),
  `sceneAssets.js` (canvas-текстуры, четыре варианта фигурок: планшет, дальномер, монтажник с киянкой, оператор рохли; подписи с обводкой).
- Цвета стеллажа по `public/works/pallet.jpg`: стойки 0x1b3b8f, балки 0xf59b1a, пятки/отбойники 0xf2c318, раскосы оцинковка. Env-map RoomEnvironment для металла.
- Таймлайн: `labels` (размеры чертежа 0.12–0.2), `installer` (0.27–0.69), `jack` (0.8–0.9, въезжает по +x), тесты 13/13.
- Мобильный: `.rack-viewport` inset 0 (весь экран), `.rack-shade` градиент под текст и кнопку; портретная камера — близко к замерщикам, отъезжает до полной ширины стеллажа к концу чертежа.
- Preview: `https://raxpro-site-git-feat-rack-hero-detail-umid-s-projects.vercel.app/ru` (uz — `/uz`). Коммит 2b22ec8.

**Решения**
- Все подписи в сцене — из модели (17×13 м, секция 2,3 м, рама 6,3 м, глубина 1,3 м) и с сайта (до 4 т на ярус); новых цифр нет.
- Бочки только на каждой пятой паллете (`variant % 5 === 2`): при трети сцена синела.
- Счётчик «мест заполнено» на телефонах по-прежнему скрыт: место занято кнопкой.

**Грабли**
- Спрайт-подпись на широком холсте 1024×192 при 104px шрифте даёт ~20px на экране — холст надо резать по ширине текста и задавать высоту в метрах сцены.
- `node --test components/hero/` падает (папка), запускать по маске `components/hero/*.test.mjs`.

**Осталось**
- [ ] Приёмка Умида; после «да» — убрать `searchParams`-переключатель в page.jsx, merge в main, `vercel --prod`.

## 2026-09-17 — выбор Умида: 3D-вариант в работу, видео сохранить

**Сделано**
- Умид выбрал вариант 1 (3D, three.js) и просит его детализировать; на мобильном key visual должен стать заметно крупнее. Вариант 3 (MP4) ему тоже понравился — сохранён.
- Сохранение видео-вариантов: ветка `keep/hero-video` + тег `hero-video-2026-09-17` (коммит 30526d7) в GitHub; исходники (4K-мастер, ключевые кадры, клипы, hero_*.mp4) — `assets-src/hero-video/` с README, папка в `.gitignore`; hero_1920.mp4 и master_4k.mp4 отправлены в «Избранное» TG.
- Постоянная ссылка на видео: `https://raxpro-site-git-keep-hero-video-umid-s-projects.vercel.app/ru?hero=mp4` (и `?hero=video`).

**Осталось**
- [ ] Детализация 3D-героя в ветке `feat/rack-hero-detail` (см. следующую запись).

## 2026-09-17 — вариант 2 героя: фотореалистичное видео по скроллу (ветка feat/rack-hero-video)

**Сделано**
- Второй метод первого экрана: 4K-мастер (замер → голограмма-чертёж → сборка → загрузка), нарезанный на 480/240 AVIF-кадров
  и промотанный скроллом. Код: `components/hero/VideoHero.jsx`, `videoTimeline.js`, `videoCopy.js`, `seq/{tier,frameStore}.ts`
  (порт скилла scroll-sequence-hero). Переключатель на одном preview: `?hero=video` (по умолчанию 3D).
- Кадры Higgsfield nano_banana_pro 4k, цепочкой от K0: K0 `a4774f86…` (пустой цех, замерщики), K3 `2c682ed3…` (L-стеллаж вокруг колонны
  в цветах RaxPro по `public/works/pallet.jpg`), K1 `702312c1…` (голограмма), K2 `8c10036b…` (полусборка), K4 `7ebf3026…` (загружен).
- Клипы seedance_2_5 5 с 1080p start→end: `3d9414bc…`, `332a6462…`, `baddaa54…`, `26a3dd1b…`; апскейл bytedance aigc 4K 24fps;
  concat `-c copy` → 484 кадра, 20,2 с. Ключевые кадры и клипы лежат в scratchpad сессии cd71b807 (`kf/`, `clips/`).
- Итого ≈ 205 кредитов Higgsfield (5 кадров × 4 + 4 клипа × 45 + 4 апскейла × ~1,6).

**Решения**
- Единственный фильтр сглаживания — Lenis; холст идёт за прокруткой напрямую, в покое доезжает до целого кадра smoothstep за 180 мс.
- Live-режим только когда первый кадр реально пришёл: без кадров показывается постер `public/seq/poster.jpg`, а не чёрный холст.
- Факты последней панели у видео другие (`videoCopy.js`: до 4 т на ярус, +60 % Drive-in) — паллето-места из rackModel относятся к 3D-модели.

**Грабли**
- Seedance без `declined_preset_id` не стартует: вместо задачи предлагает пресет «IN THE DARK».
- Порт 3005 занят dev-сервером best-audit с 12.09 — локальная проверка на 3017.
- В рабочей папке лежат незакоммиченные правки 3D-героя (14:53–15:00, не из этой сессии: rackModel.js, новая копия heroCopy) — не тронуты.

- Вариант 2б по референсу Умида (mascan-travel: один MP4 67 МБ, GOP 6, blob → `video.currentTime`): `VideoHeroMp4.jsx`,
  `?hero=mp4`, файлы `public/seq/hero_1920.mp4` (28 МБ, crf 19, `-g 6 -bf 0`) и `hero_1280.mp4` (14 МБ). Следующий seek только после
  `seeked` предыдущего. Цена формата: старт только после полной загрузки (12 Мбит/с: 23 с десктоп, 13 с телефон; кадры — 8 с / 4 с).

**Осталось**
- [ ] Приёмка Умида: 3D vs видео-кадры vs MP4 по одной preview-ссылке; после выбора убрать `searchParams`-переключатель (страница снова статическая).

## 2026-09-17 — скролл-герой: замер → чертёж → сборка → загрузка (ветка feat/rack-hero)

**Сделано**
- Первый экран заменён на three.js-сцену `components/hero/RackHero.jsx` + `rackScene.js`: человечки снимают размеры
  помещения 24×16×7,5 м (играет само), по скроллу рисуются белые линии проекта, сверху падают рамы и каждая балка,
  потом паллеты и коробки; L-образный стеллаж огибает колонну (5 + 3 секции, 74 паллето-места по расчёту).
- Таймлайн вынесен в чистую логику `timeline.js` (8 тестов: `node --test components/hero/timeline.test.mjs`).
- Тексты и цифры трёх панелей — только с текущего сайта (`heroCopy.js`, RU/UZ): 24 ч / 1000+ / 0 сум · до 4 т / Zn+RAL / 10 лет ·
  74 / +60 % Drive-in / 100 кг–4 т. Спека — `docs/superpowers/specs/2026-09-17-rack-hero-design.md`.
- Live идёт только в ветке, боевой домен не тронут: показ через preview-деплой Vercel.

**Решения**
- Единственный фильтр сглаживания — Lenis страницы; сцена привязана к прокрутке напрямую, rAF-цикл гаснет в покое.
- Reduced-motion или без WebGL → статичный кадр `/works/hero.jpg` и панели стопкой; h1 и факты — обычный DOM.
- Камера получила `shift`: на первом экране сцена уезжает вправо от заголовка, дальше влево под панели; на портрете — вверх.

**Грабли**
- `html, body { overflow-x: hidden }` ломает `position: sticky` холста → заменено на `overflow-x: clip`.
- Панель этапа на телефоне пряталась под липкой полосой «Hisoblash / Qoʻngʻiroq» → `bottom-28` до `sm`.

**Осталось**
- [ ] Приёмка Умида по preview-ссылке; после «да» — merge в main и `vercel --prod`.
- [ ] Полировка: паллеты почти не видны под коробками, коробки одного размера.

## 2026-09-05 — рендер по раскладке: фотореализм поверх 3D-модели

**Сделано**
- Кнопка «Собрать рендер по раскладке»: кабинет молча снимает 3D-сцену со стандартного ракурса
  (`RackScene.shootStandard`, 1280×720, стены включены, камера вдоль прохода) и шлёт снимок как
  референс. Higgsfield `nano_banana_pro` с `image_references` дорисовывает материалы и свет —
  ряды, проходы и ярусы остаются расчётными. Раньше кадр рисовался по словам и показывал склад,
  которого нет в смете.
- Референс наружу отдаёт `/api/kp/ref/<id>` — единственный маршрут кабинета без сессии
  (генератор провайдера кук не носит): случайный id, файл из закрытого хранилища, удаляется
  сразу после генерации. Публичный blob не годится — store приватный, `put` с `access: "public"`
  отвечает «Cannot use public access on a private store».
- Рендер встаёт в двух местах: hero на листе «Решение» и в полную ширину под планом расстановки.
- Ключ Google, если он живой, получает тот же снимок как `inlineData` — путь один на оба провайдера.
- Живой прогон на бою (`~/.claude/tools/site-qa/_kprenderlive.mjs`, пресет Fathulla): 102,7 с на кабинет,
  95,0 с на мост, кадр 283 КБ встал в документ; временный референс в хранилище удалён (0 файлов).

**Грабли**
- Кнопка рендера выключена, пока нет раскладки: в приёмке после пресета нужен клик «Рассчитать раскладку».

**Осталось**
- [ ] Цены RaxPro (пресет Fathulla расходится на 29 %) — запрос в группу

## 2026-09-05 — генератор КП: мост как служба, отказ ключа → мост, приёмка переписана

**Сделано**
- Мост поднят LaunchAgent'ом `com.umid.raxpro-kp-bridge` (KeepAlive, RunAtLoad, логи `/tmp/raxpro-kp-bridge.{out,err}`),
  раньше жил только в открытом окне терминала. `tools/kp-bridge/status.mjs` — «на связи?» по heartbeat.
  Добавлен daemon-чек в `~/.claude/bin/watchdog.py`.
- **Найдено живым прогоном:** «Прочитать набросок» на бою уходил на мёртвый ключ Google из «Ключей ИИ»
  и падал за 2,3 с «Лимит Google исчерпан»; мост задание не получал. `readSketch` и `/api/kp/image`:
  отказ ключа компании (502/504) при живом мосте → работу берёт мост. 422 (плохой снимок) не перекладывается.
- Приёмка под кабинет 01.09: `_kplogin.mjs` (вход admin), `_kptests.mjs`, `_kpmatch.mjs`, новый `_kpsketchlive.mjs`.
  Шесть КП выпускаются (🟡 только «нет рендера/плана»); менеджер/клиент совпадают
  (696 мест · 528 440 192 · 6 ряд · 58 секций · 3 яруса · 64 рамы); `_kpmeasure`: 8 листов по 296 мм, 7/7 картинок.
- Регрессии ядра (формулы 7 КП, раскладка) — все ok. `levelsFor` при 6 м и паллете 1,5 м: 2 балочных яруса =
  3 паллеты в высоту, четвёртая не влезает (5 310 + 1 500 > 5 500) — математика верна, вопрос только подписи.
- Распознавание на подписке (`sketch.local.mjs`, sketch-01.jpg): 17 рядов · 101 секция · 1 212 мест, 9 честных сомнений.

- Второй живой прогон: задание дошло до моста, но сервер отдал «не ответил вовремя» на 100-й с, мак закончил
  на 180,5 с. `maxDuration` маршрута наброска 120 → 300 (план Pro), окно `bridgeCall` для наброска 270 с,
  кнопка «Читаю набросок… до 3 минут». Коммит `95e5743`.
- Третий прогон: 2,8 с и снова «Лимит Google» — воркер звал `claude -p` через `spawnSync`, на 180 с замирал
  весь процесс вместе с heartbeat; через 60 с кабинет считал мост мёртвым. `claude()` → async `spawn`.
  Четвёртый прогон на бою: набросок прочитан через мост за 180,7 с — 49,7 × 19,5 м · 6 рядов · 2 ворот ·
  15 надписей · 9 сомнений, кнопка «Перенести в форму», «Читал: Claude».

**Грабли**
- `launchctl bootout` несуществующего агента блокирует хук безопасности — bootstrap без bootout.
- Playwright резолвится от папки скрипта: отладочные `.mjs` класть в `~/.claude/tools/site-qa/`, не в /tmp.

**Осталось**
- [ ] Цены RaxPro (пресет Fathulla расходится на 29 %) — запрос в группу

## 2026-09-05 — генератор КП: обложка в один лист, картинки в PDF, блоки видны сразу

**Сделано**
- `document.css` (print): `.kp-cover__body` 296 мм вместо 297 (замер: 297,1 мм при листе 297 →
  Chrome рвал полосу показателей и фон на 2-ю страницу); фон и затемнение вытянуты на −1,5 мм;
  `:last-of-type` вместо `:last-child` — после последнего листа шёл `break-after: page` → пустая 10-я.
- `parts.jsx` Reveal: блок в экране при монтировании показывается сразу; общий сторож по
  прокрутке любого контейнера (capture) в rAF — IO пропускал блоки при быстром листании;
  страховка 4 с. `_kpmeasure.mjs`: до правки в покое скрыто 33 из 35 блоков.
- `api/kp/pdf/route.ts`: все `<img>` → eager, прокрутка по листам, ожидание `complete` до 45 с
  и `decode()`; заголовки `X-KP-Images`, `X-KP-Images-Missing`, `X-KP-Images-Wait-Ms` для приёмки.
- Приёмка: `~/.claude/tools/site-qa/_kpmeasure.mjs` (высоты листов при media=print, reveal в покое,
  PDF через маршрут), `_kppdfpages.py --strict` (pymupdf: пустые листы, заглушки).

**Грабли**
- Preview на *.vercel.app не проверить: `redirects()` шлёт любой vercel-хост на raxpro.uz.
- Локальный вход не поднять: `vercel env pull` для development отдаёт пустые KP_SECRET/KP_PASSWORD.

**Осталось**
- [ ] Обновить `_kptests.mjs` / `_kpmatch.mjs` под интерфейс 01.09
- [ ] Цены RaxPro (пресет Fathulla расходится на 29 %)

## 2026-09-05 — диагностика генератора КП на бою перед доработкой

**Найдено** (шесть тестовых КП на raxpro.uz, PDF разобран по листам через pymupdf)
- PDF: обложка рвётся на два листа — фото и полоса показателей уезжают на стр. 2;
  причина — вложенные `min-height: 297mm` у `.kp-page` и `.kp-cover__body` плюс отступы.
- PDF: пустой 10-й лист (только шапка); портрет директора и 3 из 4 кадров объектов —
  серые квадраты, хотя `public/brand/founder.jpg` и 23 кадра на диске есть.
- Документ невидим «в покое»: `.kp-reveal { opacity: 0 }` + IntersectionObserver —
  в превью кабинета и у клиента по ссылке листы пустые до прокрутки контейнера.
- `_kptests.mjs` и `_kpmatch.mjs` устарели после редизайна 01.09: «сумма —»,
  вердикт читает меню, match падает на `.font-mono.text-[10px]`.
- Мост на маке не запущен → «Прочитать набросок» и «Собрать изображение» выключены.
- Логи Vercel через MCP — 403; `RAPID SALES` в шапке — юрлицо RaxPro, не ошибка.

**Осталось**
- [ ] Обложка в один лист, пустой хвост, ожидание картинок в PDF, reveal без пустых листов
- [ ] Обновить приёмочные скрипты + проверка PDF по листам
- [ ] Цены RaxPro (пресет Fathulla расходится на 29 %)

## 2026-09-04 — набросок читается как план расстановки, а не как коробка

**Сделано**
- `lib/kp/sketch.ts`: в ответе модели появились `rows` (прогоны с длиной, глубиной
  и ширинами секций), `zones` (упаковка, лестница, приёмка), `sectionWidth`,
  `rowDepth`, `aisle`, `mode`. `SYSTEM` расписан под реальные листы («A blok 26.6 metr»,
  «215 sm», «Qadoqlash zonasi»), `SCHEMA` строгая, `normalize()` режет по диапазонам.
- `lib/rack/layout.ts`: у `Room` появились `keepouts` — участки, отмеченные замерщиком.
  Блокируются без запаса вокруг (границу нарисовал человек) и участвуют в `obstaclesOf`.
- `lib/kp/sketch.room.mjs`: `sketchShape(s)` — одно место, где набросок превращается
  в геометрию комнаты. Кабинет теперь подмешивает её целиком (`{...base, ...shape}`).
- Кабинет и чертёж: зоны рисуются штриховкой с подписью, строка «С наброска»
  показывает зоны, режим и глубину ряда, панель распознавания — ряды и зоны.

**Решения**
- `rowDepth > 1600` → `rackDepth = rowDepth / 2`: на листе пишут глубину спаренного
  ряда («215 sm»), ядру нужна глубина одного стеллажа.
- Зона с листа = габаритная коробка многоугольника. До этого расчёт занимал
  упаковку стеллажами и завышал счёт: на тестовом листе 60 секций вместо 56.
- Шаг секции с листа становится длиной балки только при 2700/3300. «240 sm»
  балкой не станет — вместо выдумки предупреждение, что такой в прайсе нет.
- Общий шаг выводится по секциям, только если одно значение занимает 2/3 и больше.
  На sketch-02 (270/200/150 см вдоль разных стен) медиана давала 2000 — числа,
  которого на плане нет; теперь поле пустое и просьба задать руками.
- `readPolygon` возвращает сдвиг наружу: колонны, ворота, ряды и зоны едут
  вместе с контуром. Раньше сдвигался только контур.

**Грабли**
- `truck: "counterbalance"` проходил проверку и приезжал в `design()` как
  несуществующая техника — в ядре ключ `counterbal`. Сделана таблица соответствий.

**Проверено**
- `node --import ./lib/rack/ts-hook-register.mjs lib/kp/sketch.regression.mjs` — 12 тестов, зелено.
- `npm run build` — зелено.
- Живые прогоны: sketch-01 (304 с, medium, 49 × 19,5 м, 17 рядов / 103 секции),
  sketch-02 (106 с, low, пристенная раскладка, зоны «Zina» и «yuk kirish joyi»).

**Осталось**
- [ ] Цены RaxPro (пресет Fathulla расходится на 29 %)

## 2026-09-04 — мост: кабинет работает на подписке владельца

**Сделано**
- `lib/kp/bridge.ts` — очередь заданий через уже существующее blob-хранилище кабинета:
  сервер кладёт `bridge/jobs/<id>.json`, мак забирает, кладёт ответ в `bridge/out/<id>.json`.
  Heartbeat `bridge/alive.json` (60 с) — по нему кабинет решает, включать ли кнопки.
- `tools/kp-bridge/worker.mjs` — воркер на маке: `claude -p` с белым списком инструментов.
  Набросок читает Claude (тот же `SYSTEM`/`SCHEMA`, что и API-ветка), кадр рисует
  Higgsfield через MCP. Запуск: `npm run bridge` или двойной клик по
  `tools/kp-bridge/Запустить мост.command`.
- `lib/kp/sketch.ts` и `app/api/kp/image/route.ts`: если ключей компании нет, а мост жив —
  работа уходит на мост. Ключи, когда появятся, всегда в приоритете.
- Страница «Ключи ИИ» сохранена, добавлен блок «Временный режим — подписка владельца»
  со статусом на связи / не на связи.
- `KP_BRIDGE=1` в production, задеплоено.

**Проверено**
- Кадр Higgsfield сквозь мост: 63,9 с, дошёл до кабинета.
- Распознавание листа замера: 49,9 с, `confidence: medium`, 9,8 × 6,4 м, семь честных сомнений.

**Решения**
- Ключа Anthropic и Higgsfield в природе нет — есть только OAuth-подписка и MCP,
  поэтому «положить ключ в env Vercel» невозможно физически. Мост — единственный путь
  показать клиенту живую систему сегодня.
- Gemini-ключ Умида мёртв (`429 RESOURCE_EXHAUSTED`, кредиты кончились) — на него не опираемся.
- Кадр ужимается до 1600 px JPEG q82 в воркере: сырой Higgsfield весит 7,6 МБ и раздувает PDF.

**Грабли**
- macOS: `timeout` не существует, обёртку убрать.
- `vercel env pull` отдаёт KP_SECRET/KP_PASSWORD пустыми (sensitive) — войти в боевой
  кабинет из терминала нельзя, проверка входа только руками.

**Осталось**
- [ ] `SYSTEM` в `lib/kp/sketch.ts` читает контур помещения; реальные листы замера —
      это план расстановки рядов с длинами секций. Расширить схему под ряды.
- [ ] Цены RaxPro (пресет Fathulla расходится на 29 %)

## 2026-09-04 — деплой КП, автоприёмка, страница-отчёт

**Сделано**
- Задеплоено на raxpro.uz, автоприёмка прогнана: `site-qa https://raxpro.uz --send`.
- Атрибуты `width/height` проставлены картинкам главной (Header/Footer логотипы,
  постер героя, направления, founder, сертификаты, блог, OpinionsSlider) — CLS.
- `LogoMarquee`: убран дублирующийся атрибут `loading`.
- Отчёт Умиду страницей: https://claude.ai/code/artifact/21dd02d5-8334-45cc-a337-577848537d89

**Решения**
- Замечания приёмки по alt — ложные: `alt=""` стоит у декоративного постера героя
  (`aria-hidden`) и у второй копии логотипов в марки (тоже `aria-hidden`). Не трогал.
- `hero.mp4` в отчёте приёмки «неудачный» — на бою отдаётся 200 (7,8 МБ),
  браузер обрывает загрузку при закрытии страницы. Не баг.
- Title главной 99 символов — набит ключами намеренно, SEO-решение; не менял без Умида.

**Осталось**
- [ ] Цены RaxPro (пресет Fathulla расходится на 29 %), пароль кабинета для боевого PDF

## 2026-09-04 — кейсы и компания внутри КП: 22 реальных кадра, завод не назван

**Сделано**
- `lib/rack/profile.ts`: 22 снимка сданных объектов (`public/kp/cases/`, 1500px q82)
  разложены по шести продуктам; `coverFor`/`galleryFor`/`projectsFor` подбирают
  обложку, галерею и таблицу «объекты в цифрах» под продукт клиента.
- Имя завода убрано из клиентских текстов (`lib/rack/kp.ts`, листы 2 и 6) —
  формулировка «сертифицированный завод-партнёр, технология RollForm».
- Печать: `PrintContext` в `parts.jsx` + ожидание `document.images` в `/api/kp/pdf` —
  раньше кадры с `loading="lazy"` уходили в PDF серыми прямоугольниками.
- Листы 6–7 переверстаны: ритм 30/34/34, лид-кадр объектов 21/9, линии клиентов
  через `border-top` + рамку контейнера. PDF ровно 7 страниц, битых картинок нет.
- `public/kp/cases/manifest.json` пересобран из `CASES` (кодом не используется, справочник).

**Решения**
- Завод не называем до эксклюзива — прямая просьба Xurshidbek Kasimov, рабочая группа 29.08.2026.
- Проверка глазами шла локальным `scratchpad/print.mjs`: своя сессия через `makeSessionToken`,
  `/kp?print=1` — путь боевого PDF без blob-хранилища.

**Грабли**
- `marginTop:'auto'` у галереи выталкивал примечание за лист → 8 страниц вместо 7.
- Несуществующий `paymentKey` роняет `buildKp` молча (`undefined.ru`) — валидных три:
  `prepay100`, `split5050`, `installment`.

**Осталось**
- [ ] Деплой + `site-qa --send`, страница-отчёт Умиду

## 2026-09-02 — распознавание наброска проверяется подпиской, без баланса API

**Сделано**
- `lib/kp/sketch.local.mjs` — прогон распознавания на подписке Умида, без ключа API.
  Промпт и схема импортируются из `sketch.ts` (`SYSTEM`/`SCHEMA` стали `export`),
  дальше боевые `normalize()` и `design()`. Запуск:
  `node --import ./lib/rack/ts-hook-register.mjs lib/kp/sketch.local.mjs research/sketches/sketch-01.jpg [--save f.json] [--model sonnet]`,
  `--from f.json` — повтор по уже распознанному, без обращения к модели.
- Оба листа прочитаны насквозь: `sketch-01` → 50 × 19,5 м, двое ворот по 5 м,
  блоки A/B/C и зона упаковки, 19 рядов · 1452 места; `sketch-02` → 9 × 14,5 м, 156 мест.
  Два независимых прогона по `sketch-01` дали одни и те же габариты и ворота.
- `lib/kp/sketch.room.mjs` — общий `toRoom()` для регрессии и локального прогона.
- Три регрессии зелёные.

**Решения**
- Ключ RAX PRO нужен бою, а не отладке. Импорт промпта из `sketch.ts` держит проверку
  на боевом тексте: разойтись они не могут.
- Скрипт сохраняет распознанное ДО `design()`: отказ ядра — ответ ядра, а не сбой чтения.

**Грабли**
- Первый прогон шёл через `claude_headless.sh` (`--dangerously-skip-permissions`) из папки
  проекта. Фоновый claude подхватил SessionStart-контекст, «продолжил работу» и сам сделал
  коммит `789c82c`. Теперь вызов идёт напрямую: `--allowedTools Read`,
  `--disallowedTools Bash,Write,Edit,…`, `cwd = /tmp` — контекст проекта не подтягивается.
- Своя копия `toRoom()` в скрипте потеряла `rackDepth` — `design()` молча не нашёл ни одного
  ряда и отказался считать исправную комнату 50 × 19,5 м. Отсюда общий модуль.
- «30 metr» на листе мерит только левую зону до ворот, не всё здание: полная ширина не
  подписана вовсе. Модель увела это в `warnings` — так и должно быть, иначе счёт уедет вдвое.

**Осталось**
- [ ] Ключ Anthropic с балансом — HTTP-ветки (401/429/400) и tool_use подпиской не покрыты
- [ ] Цены от RaxPro: пресет Fathulla расходится на 29 %

## 2026-09-01 (утро) — прогон кабинета КП на боевом: путь сотрудника целиком

**Сделано**
- Прошёл `https://raxpro.uz/kp` насквозь под `admin`: вход, ключи, набросок,
  расчёт, реестр, PDF, ссылка клиенту. Всё, кроме самого ответа модели, работает.
- **Ключи ИИ**: подключение проверено сквозняком — ключ шифруется, ложится в blob,
  переживает перезагрузку страницы и подхватывается `/api/kp/sketch`
  (`Ключ Google не принят` вместо `не подключён ключ ИИ`). Тестовый ключ отключён,
  оба провайдера снова «НЕ ПОДКЛЮЧЁН».
- **PDF на боевом**: `POST /api/kp/pdf` → 200, `application/pdf`, 653 925 байт,
  7 листов, `KP-260901-803.pdf`, 6.2 с холодным стартом.
- **Реестр**: КП сохранилось, статус меняется, «Открыть» возвращает документ
  в редактор. `GET /api/kp/docs` — 1 документ на 250 373 760 сум.
- **Ссылка клиенту** (демо, живая):
  `https://raxpro.uz/tp#TU67csMwDPsXzHSPtuzY4QekS4cOnbqpshznolg-…` — `curl https://raxpro.uz/tp`
  без cookie отдаёт 200, `/kp` без cookie отдаёт форму входа. 7 листов, 279 паллето-мест.
- Три регрессии зелёные: `lib/rack/regression.mjs`, `layout.regression.mjs`,
  `lib/kp/sketch.regression.mjs`.
- **Починено** (`lib/kp/sketch.ts`): ошибка провайдера теперь всегда пишется в лог
  сервера, а 400 от Anthropic разделён на «отклонён снимок» и «отклонён сам запрос».

**Решения**
- 400 от Anthropic раньше всегда читался менеджеру как «переснимите файл».
  Если 400 придёт из-за схемы инструмента или модели, менеджер будет
  бесконечно переснимать лист, а в логах Vercel не останется ничего:
  ветки 401/403/404/429/400 не логировались вовсе. Теперь логируется каждая.
- Ключи Anthropic/Google в кабинет не вводил: настоящий ключ подключает Умид сам.
  Проверял механику заведомо нерабочим ключом — путь «сохранить → живая проверка →
  честный отказ → подхват в распознавании» это покрывает целиком.

**Грабли**
- Проверка формы запроса к Google идёт **раньше** квоты: битое поле даёт 400,
  а нынешнее тело распознавания на том же ключе даёт 429. Значит
  `systemInstruction` + `responseJsonSchema` Google принимает, упирается только в деньги.
- `vercel env pull` отдаёт `KP_PASSWORD` и `KP_SECRET` как `""` (sensitive).
  Пароль — в memory-файле проекта, `KP_SECRET` — в `.env.local`, и он совпадает с боевым.
- Пресет Fathulla считает 223 548 000 против фактических 172 711 892 (+29.4 %) —
  это заложенный сигнал «цены за единицу устарели», а не ошибка расчёта.

**Осталось**
- [ ] Ключ Anthropic с балансом — без него распознавание наброска не проверить ничем
- [ ] Пополнить кредиты Google (429 prepayment credits depleted) — молчит «Собрать изображение»
- [ ] Первый живой прогон распознавания: смотреть логи Vercel на 400 от Anthropic
- [ ] От RaxPro: свежие цены за единицу (пресет расходится на 29 %)
- [ ] От RaxPro: прайсы на торговый/набивной/мезонин, правило по анкерам


## 2026-09-01 — кабинет уезжает RaxPro: свои ключи, реестр, набросок, новый документ

**Сделано**
- **Кабинет как продукт клиента.** Вход по логину на каждого сотрудника
  (`admin` завёлся из `KP_PASSWORD`, дальше только логины), реестр выпущенных КП
  в приватном Vercel Blob со статусом и автором, раздел «Сотрудники».
- **Свои ключи ИИ.** RaxPro подключает свой Anthropic и свой Google в разделе
  «Ключи ИИ»: AES-256-GCM под `KP_SECRET`, наружу только `sk-ant-…7f2a`.
  **Модель выбирается в кабинете** (`lib/kp/models.ts`) — счёт их, выбор их.
- **Документ клиенту переделан:** 8 листов, обложка по продукту, лист компании
  с фото основателя и сертификатами завода, лист сданных объектов, клиенты
  набором, полоса «за что вы платите» на спецификации.
- **Типографика:** Golos Text + Source Serif 4. Manrope выкинут. Правило —
  гротеск считает (обложка, цифры, таблицы), сериф говорит (заголовки листов,
  лид, цитата, подписи). Кириллица обеих проверена запросом к
  fonts.googleapis.com **до** установки.
- **Лист A4 на телефоне** масштабируется целиком через `--kp-fit` (zoom,
  меряется родитель в `DocRoot`), а не переверстывается: клиент обязан видеть
  тот документ, который менеджер распечатает.
- Регрессия распознавания: `node --import ./lib/rack/ts-hook-register.mjs
  lib/kp/sketch.regression.mjs` — 6 сценариев, весь путь «ответ модели →
  раскладка», без денег на провайдера.

**Решения**
- **Проверка ключа бьёт настоящей генерацией**, а не `GET /v1/models`:
  список моделей у обоих провайдеров отвечает 200 на нулевом балансе,
  и кнопка «Проверить» врала бы, а падало бы уже при клиенте.
- Модель Google — псевдоним `gemini-pro-latest`, не версия.
- Реестр хранит **ввод формы**, а не отрендеренные страницы: КП через год
  пересчитается тем же ядром.

**Грабли**
- `gemini-2.5-pro` отвечает 404 «no longer available to new users» — на новом
  ключе RaxPro ветка Google упала бы в первый день.
- Сторона колонны «400» читалась `mm()` как 400 метров и обрезалась до 1500:
  один и тот же набросок давал 60 секций из метров и 52 из миллиметров.
  Поймала регрессия, лечится `smallMm()`.
- Форма входа до гидратации уходила нативным сабмитом — сотрудник видел бы
  мёртвую кнопку. Кнопка теперь ждёт монтирования («Загружаю…»).
- `vercel --prod` из CLI тянул 900 МБ `research/`: нужен `.vercelignore`.
  Быстрее и надёжнее деплоить пушем в GitHub.
- Сумма 1 264 488 960 ломала ячейку на второй строке — кегль теперь зависит
  от длины числа (`statSize`).
- **Проверить распознавание вживую не удалось:** ключа Anthropic на машине нет,
  у Gemini кончились предоплаченные кредиты (429 «prepayment credits are
  depleted») — это же ломает `nano_gen.py` и генерацию картинок.

**Осталось**
- [ ] Первый живой прогон распознавания наброска на ключе RaxPro
- [ ] Пополнить кредиты Google — иначе кнопка «Собрать изображение» молчит
- [ ] От RaxPro: прайсы на торговый/набивной/мезонин, правило по анкерам

## 2026-08-25 (вечер) — генератор КП: драфт, 3D, PDF, ссылка клиенту

**Сделано**
- **Обводка по фото драфта.** Замерщик фотографирует набросок, менеджер задаёт
  масштаб отрезком с известной длиной, обводит контур, ставит колонны и ворота.
  Раскладка идёт по настоящей форме склада. Редактор открывается на весь экран —
  в колонке 400 px склад не обвести. Проверено на L-образном складе 32×21 м:
  8 рядов, 70 секций, 840 паллето-мест; в вырезе стеллажей нет.
- **Ядро с произвольным контуром** перенесено с Python-версии и сверено с ней
  до секции. Регрессия: `node --import ./lib/rack/ts-hook-register.mjs
  lib/rack/layout.regression.mjs`.
- **Three.js внутри КП** — модель из той же раскладки. Кнопка «Снять кадр в КП»
  кладёт ракурс на лист «Решение» и уезжает в PDF.
- **Кнопка «Скачать PDF»** — `/api/kp/pdf` открывает `/kp?print=1` в headless-chromium.
  Холодный старт ~26 с, 5 страниц, текст живой.
- **Ссылка для клиента** `raxpro.uz/tp#…` — ~490 символов, **без хранилища**.
  Документ целиком в адресе, раскладка пересчитывается ядром.
- **Blender по сцене из генератора** — `research/rack-calc/render_kp.sh`, три ракурса.

**Грабли**
- Даты: сервер в UTC, браузер в Ташкенте → после 19:00 React ронял гидратацию
  (418). **На localhost не воспроизводится.** Лечение — `Intl` с `timeZone`.
- Ссылка клиенту давала 60 секций вместо 58: колонны разворачивались только
  в форме. Теперь обе стороны идут через `roomWithColumns()`.
- Playwright не находит `<select>` внутри `<label>` без `for/id` — починено
  через `useId`, заодно доступность.
- Cookie доступа была `Path=/kp` и не отправлялась на `/api/kp/pdf`.
- `@sparticuz/chromium`: нужен `outputFileTracingIncludes` для бинарника.
- Снимок сцены пустой без `preserveDrawingBuffer: true`.
- Своё же правило приёмки блокировало сгенерированный текст.

**Осталось**
- [ ] Higgsfield поверх снимка сцены — нужен аккаунт, кредиты, ключ
- [ ] Отдельные входы сотрудникам, реестр выпущенных КП
- [ ] От RaxPro: прайсы на три продукта, цены за единицу, правило по анкерам, смета

## 2026-08-25 — разобрана группа RaxPro, собран генератор КП

**Сделано**
- Выгружена и прочитана вся группа «Raxpro & Prototype AI» (`-1004302661621`):
  7 тем = 6 продуктов, 194 сообщения, 136 фото, 7 КП в PDF, 4 файла .max (822 МБ).
  Сырьё — `research/tg-group/` (в git не идёт), разбор — `research/kp-knowledge/product-line.md`.
- **Генератор КП** живёт на **raxpro.uz/kp** за паролем (`KP_PASSWORD` в Vercel,
  сейчас `RaxPro-KP-2026`). Ядро — `lib/rack/`: справочник компании, каталог из
  6 продуктов, формулы спецификации, цена с НДС 12 %, приёмка, раскладка склада.
- **Формулы спецификации** проверены регрессией на 6 фактических КП:
  `node lib/rack/regression.mjs`. Седьмое (Lion Print, 843 млн) оставлено как
  заведомо битое — генератор обязан его отклонять.
- **Модель цены «по прайсу за секцию»** для среднегрузового и архивного:
  прайс линеен, раскладывается на цену рамы по высоте и цену яруса по длине балки;
  ряд делит рамы между соседями. На КП BLOOMSHOP даёт 209 294 494 сум с НДС
  против 209 295 000 в документе.
- **Раскладка по габаритам** перенесена с Python-ядра: план рисуется SVG прямо
  в КП, ждать 3ds Max больше не нужно. Высота рамы ограничена 6000 мм.
- Шесть тестовых КП, по одному на продукт — `research/kp-tests/` (PNG + PDF).
- Инструкция — `docs/KP-GENERATOR.md` и страница
  https://claude.ai/code/artifact/25b7a746-9485-462d-82e7-dd2641c45f75
- Отчёт — https://claude.ai/code/artifact/2adc3893-ffd7-45ac-a499-b50e738eb607

**Решения**
- Факты о компании (ISO, стаж, число клиентов, реквизиты) живут только в
  `company.ts`. В семи фактических КП они разошлись именно потому, что
  правились руками в теле Word-файла.
- Каждое правило приёмки соответствует конкретной ошибке в фактическом КП,
  и в коде рядом с правилом указано, в каком именно.
- Скидка без причины не печатается: в трёх КП скидка стояла без обоснования.
- Паллето-места и цена за место вынесены на первую страницу — этой величины
  не было ни в одном КП, а только по ней клиент сравнивает поставщиков.

**Грабли**
- Даты: сервер Vercel в UTC, браузер в Asia/Tashkent. После 19:00 UTC React
  ронял гидратацию (ошибка 418) на всех шести КП. **На localhost не
  воспроизводится** — поймано только прогоном тестов по боевому адресу.
  Лечение: `Intl` с явным `timeZone` в `dateParts()`.
- Playwright не находит `<select>` внутри `<label>` без `for/id` — заодно
  чинит доступность. `Field` теперь связывает подпись и поле через `useId`.
- Подгонка цен за единицу для среднегрузового давала расхождение 14 %.
  У них в КП колонка «Цена» пустая — считать надо по прайсу за секцию.
- Собственное правило приёмки блокировало сгенерированный текст: проверка
  «число клиентов вписано руками» ловила и подстановку из справочника.
  Правила фактов теперь проверяют только то, что ввёл менеджер.
- `research/tg-group/` — 866 МБ, добавлено в `.gitignore`.

**Осталось**
- [ ] Прайс на торговый, набивной и мезонин — три продукта из шести без цен
- [ ] Цены за единицу от завода на паллетный (сейчас обратным счётом из КП)
- [ ] Уточнить, когда 4 анкера на раму, а когда 8
- [ ] Реестр выпущенных КП: сейчас JSON выгружается вручную

## 2026-08-21 — калькулятор КП: раскладка, спецификация, 3D

**Сделано**
- Разобраны 6 фактических КП RaxPro (Kusum $211 620, Sika, Fathulla, Toshkent.uz,
  Lion Print). Вывод: четыре из них — один Word-шаблон, 29 из 33 картинок общие,
  меняются только 1–3 кадра (план из 3ds Max + рендер) и таблица цены.
- **Восстановлены формулы спецификации** из самих КП: рамы = секции + ряды,
  балки = секции × ярусы × 2, замки = балки × 2, анкеры = рамы × 4.
  Регрессия 7/7 на реальных данных — `research/rack-calc/calc_v1_regression.py`.
- Ядро v2: контур полигоном, колонны и ворота координатами с буферами,
  перебор ориентации и смещения, пристенный режим. Тест поймал ошибку:
  перебор ставил ряд без прохода. После проверки доступа — 9 рядов и
  1 995 мест против 1 830 у прямоугольной версии (+165 мест на том же складе).
- Протестированы 4 способа 3D на одних данных: Three.js (интерактив, точность),
  Blender headless (8 сек/кадр), генерация с нуля (**провал**: 6 рядов вместо 8),
  Blender + генерация по референсу (**лучший кадр**, геометрия сохраняется).
- Собрано демо-КП: визуализация, план, интерактивная модель, смета, данные завода.
  Дизайн — индустриальный монохром, автоприёмка 320/768/1440 чистая.
  Ссылка: https://claude.ai/code/artifact/81352dde-6ef3-4316-b4ab-1b6b27c8d97d
- Схема интеграции на сайт: https://claude.ai/code/artifact/5c8bd01d-e6e6-484a-8561-025188c76ea3
  Разбор КП: https://claude.ai/code/artifact/7687f7cd-2776-4dc8-b6d8-5132928a2c45

**Решения**
- Ядро для сайта писать на **TypeScript** в `lib/rack/`, не дублировать логику:
  один код считает и в браузере (калькулятор), и на сервере (выпуск КП).
- Генеративные картинки — только **поверх посчитанного рендера**, не с нуля.
  С нуля модель врёт в количестве рядов, а рядом лежит точная смета.
- Публично показываем паллето-места и вилку цены; спецификацию по позициям
  и точную сумму — после контакта. Конкуренты читают сайт (Хуршид сам
  сравнивает с prostellaj.uz).
- Первым делать **кабинет менеджера**, не публичный калькулятор: если расчёт
  врёт, это должен увидеть менеджер, а не рынок.

**Грабли**
- Blender на Vercel не запустится (нет места под пакет, нет времени, нет диска) —
  нужна отдельная машина. До неё кадр берётся из Three.js через canvas → PNG.
- В КП клиента тиражируются ошибки: «ISO g001» вместо 9001 во всех пяти,
  опыт компании то 2 то 3 года в одном файле, клиентов 150/200/300,
  у Lion Print балка «33000×1200» (33 метра) в КП на 843 млн,
  у Kusum в шапке чужая компания «Rapid Sales». Всё это сидит в константных
  страницах и чинится один раз.
- У «Toshkent.uz» анкеров 144 при 18 рамах (×8 вместо ×4) — не подгонять,
  система должна поднимать флаг.

**Осталось**
- [ ] Порт ядра в `lib/rack/` на TypeScript
- [ ] Получить от RaxPro: спецификации с параметрами складов, исходники 3ds Max,
      закупочный прайс, правило скидки
- [ ] Алгоритм раскладки проверить не на чем — нужна пара «контур + расстановка»
- [ ] Хранилище для КП по ссылке (сейчас у сайта базы нет)

## 2026-08-05 — политика конфиденциальности и цена без приставки «от»

**Сделано** (требования Repid по Merchant Center, группа «Prototype & Raxpro» 04.08)
- Новая страница `/politika-konfidencialnosti` (RU + UZ) — Merchant Center требует
  её отдельным адресом. Текст написан по фактам сайта, не по шаблону: имя,
  телефон, адрес и текст заявки → Telegram + Bitrix24; аналитика GA4 и GTM
  (обезличенно); корзина в `localStorage` и на сервер не уходит. Ссылка
  добавлена в футер обоих языков, в sitemap попадает автоматически
  через `POLICY_SLUGS`.
- Убрана приставка «от …» / «… dan boshlanadi» из цены в каталоге и на карточке
  товара. Google Shopping не принимает цену с приставкой: значение на странице
  должно совпадать с фидом, иначе товар уходит в отклонённые.

**Решения**
- Смысл «цена не финальная» не потерян, но вынесен из самой цены: над числом
  подпись «Цена за эту конфигурацию», под ним — «Другие размеры и количество
  ярусов — считаем по замеру». Ислombek просил «dan boshlab», Repid просил
  убрать — так закрыты оба требования, а число осталось машиночитаемым.

**Осталось**
- [ ] Товаров всего 3, Merchant Center просит 10–15. Нужны цены от клиента.
- [ ] Хуршид (04–05.08) сравнивает нас с prostellaj.uz: хочет, чтобы по запросам
      выходили фронтальные и набивные стеллажи, а не только среднегрузовые, и
      чтобы в выдаче были картинки/звёзды. Это Ads + отзывы, мяч у Ойбека.

## 2026-08-03 — каталог с ценами, корзина и фид под Google Shopping

**Сделано** (запрос Ойбека @Oybek_0487, сообщения 603421/603422 в группе «Prototype & Raxpro» 5481171205)
- `lib/products.js` — 3 SKU с реальными ценами от клиента (msg 603551-603553 от 02.08):
  `RX-PAL-4027-3` 7 032 128 · `RX-SRG-2020-4` 3 620 000 · `RX-ARH-2010-4` 1 200 000 сум.
- Страницы (все статические, RU+UZ): `/katalog`, `/katalog/[slug]`, `/korzina`,
  `/dostavka-i-oplata`, `/vozvrat-i-obmen`, `/publichnaya-oferta`, `/kontakty`,
  `/o-kompanii`, `/zakaz-prinyat`.
- Корзина: `CartProvider` (localStorage) + `AddToCart` + `CartLink` (бейдж в шапке) + `CartView`.
  `CartProvider` подключён в `app/[lang]/layout.jsx` — счётчик переживает переходы.
- `/api/order` → карточка заказа в Telegram (те же кнопки статусов, что у `/api/lead`) + Bitrix `crm.lead.add`
  с `OPPORTUNITY`/`CURRENCY_ID=UZS`. Номер заказа `RX-YYMMDD-NNNN`.
- Фиды Merchant Center: `/feed.xml` (RU) и `/feed-uz.xml` (UZ), генератор `lib/feed.js`.
- `productOfferSchema` в `lib/schema.js` — Product + Offer с ценой; ItemList на каталоге.
- Меню: добавлен «Каталог», «О компании» и «Контакты» переведены с якорей на отдельные страницы.
  Футер: новая колонка «Покупателю» со всеми политиками.
- Фото товаров: студийные рендеры на белом (`public/products/*.jpg`, 1200×1200) под цвета реальной
  продукции — синие стойки + оранжевые балки, архивный белый.

**Решения**
- Цены только реальные, от клиента. Расчётные/выдуманные не добавляем: расхождение цены в фиде
  и на странице = блокировка аккаунта Merchant Center.
- Сервер пересобирает заказ из `lib/products` по артикулу — цена из тела запроса игнорируется.
- `g:shipping` в фид НЕ пишем: `g:region` принимает только регионы, заведённые в Merchant Center.
  Правило «Ташкент бесплатно / регионы по расчёту» настраивается в аккаунте (задача Ойбека).
- Срок возврата — **10 дней** (ст. 18 Закона РУз «О защите прав потребителей»), не 14 как в РФ. Проверено.
- Категория Google — `5833` (Business & Industrial > Industrial Storage > Industrial Shelving),
  сверено с официальной таксономией.
- `/korzina` и `/zakaz-prinyat` закрыты `noindex`.

**Грабли**
- nano_banana устойчиво рисует 5 полок вместо 4 — помогло только явное перечисление
  («одна сверху, одна на треть, одна на две трети, одна снизу»), с 3-й попытки.
- `formatPrice` использует неразрывный пробел (` `) — в тестах сравнение с обычным пробелом
  даёт ложный провал, нормализовать.
- Кнопка «В корзину» на 2 сек меняет текст на «Добавлено ✓» и выпадает из локатора Playwright —
  `nth()` попадает в соседний товар. Ждать 2.2 с между кликами.
- Локальный тест заказа: переопределять `LEAD_CHAT_ID=361944621`, иначе тестовые заказы падают
  в боевую группу клиента «RaxproLeads».
- Перед пушем всегда `git fetch && git pull --rebase` — их программист пушит параллельно
  (в этот раз забрал 2 коммита `da200b8`, `e853dc4`).

**QA** — build чистый; Playwright 390/768/1440 × 14 страниц: 0 ошибок консоли, 0 overflow, по 1 h1;
сквозной тест корзины 12/12 (сумма, валидация, отправка, очистка, защита от подмены цены);
оба фида проходят XML-разбор, по 3 товара.

**Осталось**
- [ ] Полный прайс от клиента (размеры/ярусы/цена) → добавить SKU пачкой в `lib/products.js`.
- [ ] Предметные фото реальной продукции на белом фоне от Муродбека → заменить рендеры.
- [ ] Ойбеку: подключить фид в Merchant Center, настроить доставку на уровне аккаунта,
      переотправить sitemap в GSC.
- [ ] Юр. реквизиты компании для оферты (ИНН, банковские реквизиты) — сейчас страница без них.
