# HANDOFF — raxpro-site (состояние на 22.09.2026 13:00)

**Ветка:** `feat/site-v3`, HEAD `31ad00d`, всё запушено. `main` не трогать.
**Preview:** https://raxpro-site-git-feat-site-v3-umid-s-projects.vercel.app/ru (uz — `/uz`). Собирается автоматически из пуша, ~1 мин.

## 24.09 — новая ветка `feat/structure` (структура как у Prostellaj)
ТЗ: https://claude.ai/artifact/QB3377PUAU3N3js4h1kq9B · Preview: https://raxpro-site-git-feat-structure-umid-s-projects.vercel.app/ru/napravleniya (за логином Vercel — site-qa гонять локально, `next start -p 3123`).
Готово: этапы 1–5 — хаб и 8 торговых подкатегорий; выбор полок (архивные 3–6, фид с вариантами); портфолио 14 кейсов + шоурум; 11 страниц «по назначению», мезонин, услуги/монтаж, покупателям/гарантия. HEAD 286781a.
Этап 6 готов: 19 своих обложек + фото архивного меняется с числом полок. Этап 7 (c7824c4): вычитка UZ — yuklagich/katak/yetkazib berish/montaj, кальки, апострофы; RU чистый. Дальше: 8 — сравнение с Prostellaj.
Ждём от Умида ответы 1–7 из ТЗ (цена среднегрузовых 3 620 000 vs 4 344 966) и 2–3 фото интерьера шоурума от Муродбека → `INTERIOR` в app/[lang]/shourum/page.jsx.

## На чём остановились (22.09, ночь — сессию закрыли по просьбе Умида)

**Задача в работе:** архитектура генератора КП на все виды стеллажей — от проекта до визуализации, с GPT внутри.
Умид просил: сотрудник заходит в генератор → вводит данные или шлёт эскиз → GPT проектирует → GPT визуализация → Higgsfield фотореалистичный кадр. GPT подключать **через подписку, не по токенам**.

**Что уже выяснено (подробности — верхняя запись WORKLOG.md):**
- Мост на подписку владельца уже работает: `lib/kp/bridge.ts` + `tools/kp-bridge/worker.mjs` + LaunchAgent `com.umid.raxpro-kp-bridge`. Виды работ: `sketch`, `image`.
- Codex CLI авторизован по подписке (`auth_mode = chatgpt`, ключа API нет). Есть `--output-schema` — ответ строго по JSON-схеме.
- Дыра в ядре: паспорт типа (`lib/rack/catalog.ts`) описывает габариты и позиции, но не правила раскладки; `spec.ts` написан под паллетный фронтальный и применяется ко всем шести типам. Мезонин в нынешнюю `Geometry` не ложится.

**Следующий шаг:** собрать страницу-архитектуру для Умида из верхней записи WORKLOG (он ждёт ссылку, а не простыню), затем — раздел «правила раскладки» в паспорте типа.

## Что сделано за 22.09 (ночь → день)
- Bento «Почему RaxPro»: реалистичные SVG-стеллажи (`components/RackFigures.jsx`).
- Убраны: полоса «10 лет гарантии», счётчик отзывов, «Что будет после заявки» как экран (суть — колонкой у формы), плашки-метки над заголовками, блок «RAXPRO в цифрах» на `/experts` (+ его ключи из i18n).
- «Мы не производим»: 91 замена по всему сайту → «поставляем со склада в Ташкенте». «Только мы» про гарантию → «вдвое больше, чем обычно на рынке».
- Блог — над футером; калькулятор — после «Почему RaxPro»; `PriceBreakdown` упрощён.
- «Discovery Invest» унифицировано, «24 дня», нагрузка везде «до 4 т».
- Карта ICP по каналу @RaxPro_otziv (260 постов, 66 объектов): https://claude.ai/artifact/GmxkE4cj2VoH6nc33ZCxUH

## Заказано Умидом 23.09 (в очереди)

1. **Генератор КП, сквозная цепь:** фото чертежа → автоматическое распознавание
   → 2D-проектирование → 3D → фотореалистичный кадр через nano-banana.
   Разведка 23.09: цепь уже собрана в коде целиком —
   `SketchPanel → /api/kp/sketch` (Claude/Gemini vision) →
   `lib/rack/designer.ts` → `app/kp/RackScene.jsx` (three.js, `shootStandard()`
   снимает кадр 1280×720) → `/api/kp/image` → `gemini-3-pro-image`
   (это и есть nano-banana; Higgsfield остался только в тексте деки).
   Проверено локально на `research/sketches/sketch-01.jpg`: лист читается,
   доезжает до раскладки — 7 рядов, 91 секция, 1092 паллетоместа, 9 честных
   сомнений вместо выдуманных чисел.
   Разрывы: 3D и раскладка есть только у рядных типов (drive-in и мезонин нет);
   Blender в системе не установлен (был со смонтированного образа) — но боевой
   путь его и не требует, 3D рисует three.js в браузере.
2. **Статьи с raxpro.uz → на v3, без потери SEO.** Перенести все материалы
   один в один: те же адреса, заголовки, разметка, внутренние ссылки.

## Ждёт решения Умида
- [ ] 4 т или 3,5 т нагрузка — подтвердить у Хуршида (через Умида).
- [ ] «Отгрузка со склада 1–2 рабочих дня» — PLACEHOLDER, подтвердить.
- [ ] Чип в шапке «Ответим за 5 минут» — добавлять ли «в рабочее время».
- [ ] PLACEHOLDER-цифры: bento, кейсы, «нам доверяют» (4,9★/120 отзывов, 12 регионов).
- [ ] `components/MobileCta.jsx` не используется — удалить только с согласия.

## Следующий шаг после «да»
Убрать `searchParams.hero` на главной → merge `feat/site-v3` в `main` → `vercel --prod` → `node ~/.claude/tools/site-qa/site-qa.mjs https://raxpro.uz --send`.

История — WORKLOG.md (читать верхнюю секцию).
