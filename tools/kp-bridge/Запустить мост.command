#!/bin/bash
# Двойной клик — кабинет raxpro.uz начинает читать наброски и рисовать кадры
# на подписке владельца. Окно не закрывать. Останов — Ctrl+C или закрыть окно.
cd "$(dirname "$0")/../.."
exec npm run bridge
